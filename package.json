{
  "name": "api-rest",
  "version": "1.0.0",
  "main": "src/app.js",
  "scripts": {
    "env": "rm -f .env && cp .env.example .env",
    "start": "npm run env && nodemon src/main.js"
  },
  "license": "ISC",
  "dependencies": {
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "mongoose": "^5.12.12",
    "nodemon": "^2.0.7"
  }
}
